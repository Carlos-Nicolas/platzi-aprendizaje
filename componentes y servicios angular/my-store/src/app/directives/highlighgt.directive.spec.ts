import { HighlighgtDirective } from './highlighgt.directive';

describe('HighlighgtDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlighgtDirective();
    expect(directive).toBeTruthy();
  });
});
